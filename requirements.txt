# CRI API runtime dependencies
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
requests>=2.31
openpyxl>=3.1
pydantic>=2.6
numpy>=2.0
pandas>=2.2

# Satellite & GIS pipeline (AWS Open Data — Sentinel-2 / Landsat COGs)
pystac-client>=0.7
planetary-computer>=1.0
xarray>=2024.1
shapely>=2.0
rasterio>=1.3.9
scipy>=1.12

# ML intelligence layer (risk predictor + emissions estimator)
# ClimateBERT NLP (transformers + torch) is excluded here — too large for
# Render free-tier RAM (2 GB+). Engine runs keyword fallback until you upgrade
# the Render plan to Starter ($7/mo), then add:
#   transformers>=4.40
#   torch>=2.2  --index-url https://download.pytorch.org/whl/cpu
scikit-learn>=1.4
xgboost>=2.0
