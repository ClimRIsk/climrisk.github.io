# CRI API — only packages needed to START the server
# (numpy/pandas resolved separately via pip install ./climate_risk_engine)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
requests>=2.31
openpyxl>=3.1
pydantic>=2.6
numpy>=2.0
pandas>=2.2
